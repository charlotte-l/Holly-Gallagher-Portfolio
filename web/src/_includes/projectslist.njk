<ol reversed class="projectslist" style="counter-reset: start-from {{ projectslist.length + 1 }}">
{% for project in projectslist | reverse %}
  {% set currentProject = project.data.project %}
  <li class="projectslist-item{% if project.url == url %} projectslist-item-active{% endif %}">
    <a href="{{ project.url | url }}" class="projectslist-link">{% if currentProject.title %}{{ currentProject.title }}{% else %}<code>{{ project.url }}</code>{% endif %}</a>
    <time class="projectslist-date" datetime="{{ currentProject.publishedAt }}">{{ currentProject.publishedAt|readableDate }}</time>
  </li>
{% endfor %}
</ol>
